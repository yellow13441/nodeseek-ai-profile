# Changelog

所有重要的公开版本变化记录在这里。

项目目前仍处于快速迭代阶段，版本变化不仅包含 UI 和功能，也包含提示词、交易判断规则和异步任务架构的调整。

---

## [2.8.0] - 2026-08-19

### Added

- 快速画像与自定义画像增加独立的“自动包含管理记录”开关，默认开启：
  - 管理记录在模型调用前查询，并以 `M1`、`M2` 等独立证据编号送入 Prompt。
  - 不论模型是否提及，查询状态与记录详情都会在结果中确定性展示。
  - 即使有效主题 / 评论少于最低行为样本门槛、模型被跳过，管理记录仍会独立显示。
- 每个 AI Provider 分别增加三类最大输出 Token：
  - 快速画像：默认 12,000
  - 自定义画像：默认 16,000
  - 深度交易分析：默认 32,000
  - 可配置范围为 2,000–65,536；界面明确说明该值包含模型可能使用的 reasoning Token。
- 每个 AI Provider 分别增加三类请求超时（30–900 秒）：
  - DeepSeek / OpenAI 官方默认 180 / 240 / 360 秒。
  - 第三方 OpenAI-Compatible 默认 300 / 360 / 600 秒。
- Model 改为常用下拉项 + “其他（手动输入）”：
  - DeepSeek：`deepseek-v4-flash`、`deepseek-v4-pro`
  - OpenAI / 第三方 OAI：`gpt-5.6-sol`、`gpt-5.6-terra`、`gpt-5.6-luna`、`gpt-5.6`、`gpt-5.5`
- 图片分享新增四个 Provider，与原有 16 图床组成五图床支持：
  - NodeImage
  - ImgBB
  - FreeImage.host
  - Catbox
- 分享窗口增加图床选择、Provider 独立首次上传提示，以及 PNG 内存复用；失败后可手动重试或改选服务。
- 图床上传历史记录 Provider、资源 ID 与删除模式，并按各服务真实能力提供：
  - 16 图床 / NodeImage：API 删除
  - Catbox：仅绑定 User Hash 的上传可 API 删除
  - ImgBB：打开服务返回的删除网页
  - FreeImage.host / Catbox 匿名上传：仅删除本地历史
- 图床设置增加两种分享选择策略：
  - 固定使用默认图床。
  - 在已配置凭据的图床间均衡随机轮换；每轮每家最多一次，并尽量避免跨轮连续重复。
  - 匿名 Catbox 不自动加入轮换池；池为空时回退默认图床。
- 图床设置增加单轮连通性测试：
  - 在浏览器本地生成 `360×280`、通常约 300 KB 且不含论坛内容的随机 PNG。
  - 分别显示上传与删除耗时。
  - 16 图床、NodeImage、绑定 User Hash 的 Catbox 测试 API 删除。
  - ImgBB 测试强制 60 秒自动过期；FreeImage.host 上传前提示无法 API 清理。
  - 删除失败或无法清理的测试图片写入最近上传，避免残留地址不可追踪。
- 用户名旁下拉菜单增加 `⚙️ 插件设置`；打开设置不会读取该用户资料或创建分析任务。
- 结果元信息在可观测时显示首个成功请求响应时间；单次模型阶段超过两分钟会标注“本次较慢”。
- 新增可复用的临时任务窗口：
  - 在用户点击画像 / 深度分析按钮时同步创建，降低浏览器弹窗拦截概率。
  - 同一个命名窗口可并发承接多个 UID，复用时不会重新加载并中断已有任务。
  - 原页面刷新、提交回复、跳转或关闭后，任务继续执行；新页面会恢复进度、终止入口和结果。
  - 全部任务结束后自动关闭；窗口被手动关闭时明确标记中断，不自动重发模型请求。
- 新增跨页面任务记录与取消同步，任务状态只保存在 Tampermonkey 本地存储。
- 分享窗口增加非阻断的“友善分享提示”：正常分享不受限制；被画像者明确反对继续公开时，建议移除帖子图片并按图床能力删除远端副本。

### Changed

- 第三方 OpenAI-Compatible 的新安装默认模型改为 `gpt-5.6-sol`；迁移不会覆盖用户已经保存的 `gpt-5.6` 或其他自定义模型。
- 输出预算耗尽 / 只有 reasoning 时，最多扩容重试一次；新预算为 `min(65536, max(current + 8000, ceil(current × 1.5)))`，不会降低当前预算。
- 网络错误和请求超时不再自动重试，避免供应商已经生成但本地未收到响应时发生重复计费。
- 第三方 OAI 参数不兼容时，按 API URL + Model 在当前会话记住兼容模式；之后直接使用兼容参数。
- 第三方 OAI 连接测试超时放宽到 60 秒，官方 Provider 保持 30 秒；界面说明短测试不代表长上下文 / 高 reasoning 的速度。
- 快速画像一句话 Prompt 强制优先组合账号阶段、活动结构反差和重复出现的具体对象 / 行为，并增加 `one_liner_evidence`。
- 新号、零主题或高评论量不再允许单独推导为“小号”或“刷等级”；管理记录进入一句话时必须明确使用“管理记录显示……”的来源措辞。
- 等待提示拆分为画像、自定义画像、深度交易与社区提示池；每个任务只洗牌一次，约每 6 秒轮换，比例约为产品能力 50%、模式提示 35%、社区常识 15%。
- 主 `AI画像` 按钮会优先恢复该 UID 上次查看且已有结果的模式；只有深度结果时不再自动新建快速画像。
- 分享上传超时不会自动切换下一家图床，避免产生多个无法确认的公开副本。
- 均衡轮换只在打开分享窗口时选择一次；当前窗口内重试保持同一 Provider，手动改选也不会改写默认设置。
- 图床测试上传成功后不再重新下载图片直链，避免 CDN、防盗链或反自动化策略造成无参考价值的长时间等待。
- 本地结果缓存升级为 v2.8 签名，并加入管理记录开关与当前模式输出预算。
- 完成结果缓存由单标签页 `sessionStorage` 扩展为 Tampermonkey 跨窗口存储，保持原有 30 分钟有效期，并自动清理过期和超量记录。
- 浏览器拦截临时任务窗口时，无阻断地退回当前页面执行，只在画像面板内说明刷新限制。

### Fixed

- 修复分析模式卡片的通用 `label` 布局覆盖复选框样式，导致快速画像 / 深度交易的管理记录开关与文字被拆成两行的问题。
- 修复画像 / 深度分析仍在运行时，提交回复或刷新 NodeSeek 页面会丢失任务状态并需要重新调用模型的问题。
- 修复同一原页面连续启动第二个用户任务时，复用临时窗口可能没有及时接收新队列记录、长期停在“正在启动临时任务窗口”的问题。
- 修复尚未被 worker 接收的排队任务无法立即终止、刷新后仍停在“正在终止查询”的问题；这类任务现在会直接取消并明确说明尚未调用模型。
- 修复 NodeImage 删除调用了不存在的单数路由 `/api/image/{id}` 而返回 HTTP 404；现优先使用 API-Key 文档的 `/api/v1/delete/{id}`，并仅在服务端明确提示路由不存在时兼容现网页面及旧文档路径。
- 16 图床删除现在原样保留上传响应中的删除地址与查询参数，并对上传后立即删除产生的 HTTP 404 做两次短延迟重试；最终失败时展示服务端响应摘要，便于区分登记延迟、凭据问题与服务端故障。

### Migration

- 设置 key 升级为 `ns-ai-profile-v2.8-settings`，自动读取 v2.7 / 早期设置：
  - 保留三套 AI Provider 的 API Key、URL、Model 与 reasoning。
  - 保留采样参数、自定义 Prompt 预设、设置窗口状态。
  - `includeModerationInDeep` 迁移到 `moderation.includeInTrade`。
  - 原 16 图床 Auth-Token 迁移到 `imageHosting.providers.sixteen.authToken`。
  - 既有设置默认迁移为“固定默认图床”，不会因升级突然改变上传目的地。
- 旧上传历史自动标记为 `sixteen` Provider 并迁移到 v2 历史结构。

### Documentation

- README 更新为 v2.8，补充管理记录、输出预算、长请求、跨刷新任务窗口、多图床、均衡轮换、连通性测试、删除能力、分享礼仪和发布前人工测试矩阵。

## [2.7.1] - 2026-08-19

### Fixed

- 将管理记录失败状态下的按钮文案从“单独重试管理记录”调整为“重新查询管理记录”，避开 Greasy Fork 的误判规则；按钮行为与脚本功能逻辑不变。

## [2.7.0] - 2026-08-19

### Added

- 新增四页签配置中心：
  - `🤖 AI接口`
  - `📊 分析模式`
  - `✍️ 自定义画像`
  - `🖼️ 图床`
- 快速画像与深度交易分析分别支持配置：
  - 历史采样策略
  - 主题 / 评论扫描页数
  - 送入 AI 的样本上限
  - 同主题评论上限与单条评论字符上限
  - 关键评论语境核验数量
- 新增四种历史采样策略：最近活动、全历史均匀抽样、随机抽样、近期 + 历史均匀覆盖。
- 分析设置增加轻量 / 中等 / 高负载提示，以及一键恢复推荐默认值。
- 新增关键评论语境核验，可补充主题标题、首帖、目标评论、引用文本和附近楼层。
- 新增自定义快速画像 Prompt：
  - 多预设保存、切换、复制与删除
  - 内置“技术兴趣与社区角色”示例
  - 一键复制 Prompt
  - 最终请求结构预览
- 画像与深度报告增加总耗时、模型等待时间和模型请求次数展示。
- 设置窗口保存上次页签、位置和尺寸；双击标题栏或 resize 手柄可恢复默认几何状态。

### Changed

- 快速画像默认优先近期活动；深度交易分析默认使用“近期 + 历史均匀覆盖”。
- 设置窗口首次打开以合适尺寸居中，标题栏拖动与右下角 resize 使用彼此独立的交互区域。
- 模型连接测试结果改为显示在当前 Provider 的测试按钮下方。
- 深度交易的风险语境核验保持固定，不允许被自定义 Prompt 完全关闭或覆盖。
- 本地结果缓存签名现在包含 Provider、Model、reasoning、采样设置与 Prompt 预设；相关配置变化后不会错误复用旧结果。
- 画像或深度分析运行期间，允许单独修改并保存 16 图床 Auth-Token；AI、采样或 Prompt 等影响当前分析结果的设置仍受保护。

### Fixed

- 修复旧设置窗口几何状态可能导致窗口在左上角以最小尺寸打开的问题。
- 修复拖动设置窗口时可能同时改变窗口尺寸的交互问题。
- 修复连接测试状态显示在整个设置页最底部、长页面中难以及时看到的问题。
- 修复保存配置后刷新当前 Provider 摘要时可能报错的问题。
- 修复快速画像二次语境复核返回结构不完整时覆盖第一次完整结果，进而把活跃用户错误显示为“样本不足”的问题；复核失败时现在保留首次完整画像。
- 收紧智能语境核验触发范围，降低普通描述被过度送入二次复核的概率。
- 修复分析任务运行时，单独补充图床 Token 也无法保存的问题。

### Documentation

- README 全面更新为 v2.7，并加入设置、快速画像、深度交易、自定义画像和图片分享的实测截图。
- v2.7 发布前测试清单全部更新为已通过。

## [2.6.0] - 2026-08

### Added

- 用户名旁增加下拉入口：
  - `🧭 快速画像`
  - `🔍 直接深度交易分析`
  - `⚖️ 管理记录`
- 支持跳过快速画像直接生成深度交易报告。
- 查询过程中增加 `■ 终止查询`。
- 终止前二次确认，并说明已发出的模型请求仍可能产生 Token 消耗。
- 已查询用户按钮增加状态颜色：
  - 默认紫色
  - 运行中琥珀色
  - 已有结果 / 缓存蓝色
- 同一 UID 在同页面不同楼层的状态同步。
- 后台用户分析完成后提供轻量完成提示。
- 快速画像和深度报告重新生成前增加确认。
- 分享按钮统一为 `🖼️ 分享`。
- 深度报告中的管理记录改为内联展开 / 收起。
- 分享图片按管理记录当前展开状态决定是否包含完整记录。

### Changed

- **重构多用户并发架构。**
- 从单一全局 `currentUid / isBusy / result` 状态，改为按 UID 隔离：
  - fast task
  - deep task
  - progress
  - result
  - abort handles
- A 用户任务运行过程中可以继续分析 B 用户。
- A 完成不会覆盖当前正在查看的 B。
- 单独终止某个用户任务不会影响其他后台任务。
- 多个并发深度分析首次触发管理记录第三方 API 告知时，共享同一个确认状态，避免重复弹窗和错误取消。

### Fixed

- 修复快速切换多个用户时可能发生：
  - A 用户结果显示到 B 用户面板
  - 当前 UID 与结果 UID 不一致
  - 底部操作按钮指向错误用户
  - 单一 `isBusy` 阻止合理并发
  等问题。

---

## [2.5.0] - 2026-08

### Added

- 浮窗标题栏支持拖拽移动。
- 增加 📌 钉住窗口。
- 钉住后点击页面其他区域不会隐藏窗口。
- 保留右下角 resize。
- 模型连接测试成功改为明显绿色状态提示。
- 增加完整结果图片分享：
  - 复制图片
  - 保存 PNG
  - 适配完整截图
  - 上传 16 图床并复制 Markdown
- 16 图床配置：
  - Auth-Token
  - 随机生成
  - 上传历史
  - 打开图片
  - 复制 Markdown
  - 删除远端图片
- 第一次上传 16 图床前提供第三方服务提示。
- 增加第三方服务致谢说明。

### Changed

- 分享截图按照：
  - 当前窗口宽度
  - 完整内容高度
  重新渲染，不直接截取可见滚动区域。
- 分享图片自动移除交互按钮、滚动条、进度和 resize 手柄。
- 长时间分析时用户可以把窗口移动到屏幕角落继续浏览论坛。

---

## [2.4.0] - 2026-08

### Added

- 配置页增加模型 `测试连接`。
- 测试会发送一次很短的请求，并显示：
  - 成功 / 失败
  - 耗时
  - Token
- 深度交易分析默认查询管理记录。
- 第一次调用管理记录第三方 API 前进行隐私说明。
- 管理记录加入深度交易分析进度。
- 面板支持拖拽调整大小。

### Changed

- 管理记录查询失败、限流、超时、格式异常只降级该数据源，不中断主流程。
- 管理记录状态明确区分：
  - 成功
  - 无记录
  - 限流
  - 查询失败
  - 用户取消
  - 设置关闭
- 管理记录显示阶段支持北京时间修正：
  - 原始数据保持不变
  - 仅转换 `actions_text` 中的 UTC 美式时间字符串

---

## [2.3.0] - 2026-08

### Added

- 图形化 AI Provider 配置。
- 支持：
  - DeepSeek 官方
  - OpenAI 官方
  - 第三方 OpenAI-Compatible
- 三套 Provider 分别保存：
  - API Key
  - URL
  - Model
  - 快速画像 reasoning
  - 深度交易 reasoning
- 设置入口：
  - Tampermonkey 菜单
  - 画像窗口齿轮按钮

### Changed

- API Key 不再硬编码在脚本源码。
- 配置通过 Tampermonkey `GM_setValue` 本地保存。
- 切换 Provider 不会覆盖其他 Provider 的 Key。

---

## [2.2.0] - 2026-08

### Added

- 普通画像和深度报告展示 Token usage。
- 尽量展示：
  - 输入 Token
  - 输出 Token
  - 总 Token
  - Prompt Cache Hit / Miss
  - Cache Write
  - Reasoning Token
- 增加 OpenAI 官方 `gpt-5.6` 示例。
- 增加第三方 OpenAI-Compatible 支持及参数兼容降级。

### Changed

- 交易语义规则修正：
  - `已收 / 已出` 属于正常论坛交易状态更新
  - 缺少第三方公开确认不会自动成为负面证据
  - 第三方完成交易回复只作为额外佐证
- 降低模型无依据怀疑用户自行更新交易状态的倾向。

---

## [2.1.0] - 2026-08

### Fixed

- 修复 WAIT_HINTS 在短时间内连续闪切的问题。
- 快速画像提示改为更适合阅读的轮换节奏。
- 丰富快速画像和深度分析等待提示。
- 修复 DeepSeek 深度分析在高推理模式下可能出现：
  - `reasoning_content` 消耗完预算
  - 最终 `content` 为空
  - 被误报为“DeepSeek 返回内容为空”
  的问题。

### Changed

- 深度模式提高输出预算。
- 遇到 `finish_reason=length` 或只有 reasoning 没有最终答案时自动扩大预算重试。

---

## 项目方向变化

这些版本的变化背后有一个比较明确的方向：

### 早期

> “让 AI 总结这个用户。”

### 后来

> “总结得对但没用，也是一种失败。”

因此项目逐渐把重点放在：

- NodeSeek 基线过滤
- 更具体的行为辨识
- 账号硬数据
- 交易上下文
- 管理记录
- Token 透明度
- 多模型兼容
- 用户操作体验
- 并发任务可靠性

而不是单纯让模型生成更多文字。
